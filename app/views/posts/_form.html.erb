<%= form_for [@board, @post] do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this board from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<p><%= f.label :title %></p>
<p><%= f.text_field :title %></p>
<p><%= f.label :content %></p>
<p><%= f.text_field :content %></p>
<p><%= f.label :upload_image %></p>
<p><%= f.file_field :avatar %></p>
<p><%= f.submit %></p>
<% end %>