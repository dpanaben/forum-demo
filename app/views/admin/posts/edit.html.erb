<h1>Editing Post</h1>

<%= form_for @post, :url => admin_board_post_path(@board, @post) do |f| %>
	<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>
<%= link_to 'Back', board_path(@board) %>